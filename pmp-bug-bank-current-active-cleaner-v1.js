(()=>{
'use strict';
const V='1.0.1-keep-variants';
const KNOWN='pmp_bug_bank_known_types_v1',EVID='pmp_bug_bank_symptom_evidence_v1',ACTIVE='pmp_bug_bank_active_bugs_v1',CLEAR='pmp_bug_bank_active_clear_until_v1';
function T(){try{return top||window}catch(e){return window}}
function docs(r,a,n){a=a||[];n=n||0;if(!r||n>10)return a;try{a.push(r);r.querySelectorAll('iframe,frame').forEach(f=>{try{let d=f.contentDocument||(f.contentWindow&&f.contentWindow.document);if(d)docs(d,a,n+1)}catch(e){}})}catch(e){}return a}
function clean(s){return String(s||'').replace(/\s+/g,' ').trim()}
function load(k,d){try{return JSON.parse(localStorage.getItem(k)||JSON.stringify(d))}catch(e){return d}}
function save(k,v){try{localStorage.setItem(k,JSON.stringify(v))}catch(e){}}
function norm(s){return String(s||'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim()}
function compactDetail(x){try{return norm(JSON.stringify(x.detail||x.raw_source||{}).slice(0,260))}catch(e){return''}}
function key(x){return [norm(x.type||x.kind||x.problem),norm(x.where||x.where_seen),norm(x.how_it_happens||x.why||x.symptom),norm(x.fix_rule),compactDetail(x)].join('|')||norm(JSON.stringify(x).slice(0,220))}
function unique(list){let seen={},out=[];(Array.isArray(list)?list:[]).filter(Boolean).forEach(x=>{let k=key(x);if(seen[k])Object.assign(seen[k],x,{seen:Math.max(Number(seen[k].seen||1),Number(x.seen||1)),last_seen:x.last_seen||seen[k].last_seen});else{seen[k]=Object.assign({},x);out.push(seen[k])}});return out}
function bankTitle(d){let b=d.getElementById('bank');return b?clean((b.querySelector('[data-bank-detail-title]')||{}).textContent||''):''}
function isLevelText(el){return /^Level\s+(?:[0-9]+[A-Z]?|30B)\s*[:—-]/i.test(clean(el.textContent||''))}
function wrapped(el){try{let cs=(el.ownerDocument.defaultView||window).getComputedStyle(el);let lh=parseFloat(cs.lineHeight)||parseFloat(cs.fontSize)*1.2||20;return el.getBoundingClientRect().height>lh*1.55}catch(e){return false}}
function currentVisual(){let rows=[];docs(T().document).forEach(d=>{let b=d.getElementById('bank');if(!b)return;let title=bankTitle(d);if(title!=='Bug Bank'&&b.querySelector('[data-bug-bank-owner-v1]'))rows.push({type:'Cross-Bank Bug Bank Surface Leak',where:'Bank / '+(title||'unknown bank'),signature:'Cross-Bank Bug Bank Surface Leak|Bank / '+(title||'unknown bank'),source:'current-active-cleaner'});if(title!=='Helper Bank'&&b.querySelector('[data-helper-bank-live-inspector-v2]'))rows.push({type:'Cross-Bank Helper Surface Leak',where:'Bank / '+(title||'unknown bank'),signature:'Cross-Bank Helper Surface Leak|Bank / '+(title||'unknown bank'),source:'current-active-cleaner'});if(title==='Continuous Run Bank'){let w=[];Array.from(b.querySelectorAll('h1,h2,h3,h4,p,b,strong')).forEach(el=>{if(isLevelText(el)&&wrapped(el))w.push(clean(el.textContent).slice(0,120))});if(w.length)rows.push({type:'Continuous Run Level Card Wrap Drift',where:'Continuous Run Bank',signature:'Continuous Run Level Card Wrap Drift|Continuous Run Bank',detail:{wrapped:w},source:'current-active-cleaner'})}});return rows.map(x=>Object.assign({bug_bank_section:'Active Bugs Found',severity:'high',how_it_happens:'A currently visible bank layout or ownership problem is still present.',fix_rule:'Run scoped owner cleanup, then recheck the selected bank surface.',last_seen:new Date().toISOString()},x))}
function suppressed(){return Number(load(CLEAR,0)||0)>Date.now()}
function cleanAll(){save(KNOWN,unique(load(KNOWN,[])).slice(-100));save(EVID,unique(load(EVID,[])).slice(-100));let current=suppressed()?[]:currentVisual();let active=unique(load(ACTIVE,[])).filter(x=>!/Cross-Bank .*Surface Leak|Continuous Run Level Card Wrap Drift/i.test(String(x.type||''))&&String(x.source||'')!=='Bug Bank visual detector'&&String(x.evidence_source||'')!=='visual detector');current.forEach(x=>active.push(x));save(ACTIVE,unique(active).slice(0,40));try{localStorage.setItem('pmp_bug_bank_current_active_cleaner_v1_receipt',JSON.stringify({type:'PMP_BUG_BANK_CURRENT_ACTIVE_CLEANER_V1',version:V,at:new Date().toISOString(),current_visual:current.length,active_suppressed:suppressed(),rule:'Exact duplicates are deduped; similar variants are preserved for grouped display.'}))}catch(e){}}
window.PMPBugBankCurrentActiveCleanerV1={version:V,scan:cleanAll,currentVisual};
try{let mo=new MutationObserver(()=>setTimeout(cleanAll,70));mo.observe(document.documentElement,{childList:true,subtree:true,characterData:true})}catch(e){}
window.addEventListener('load',()=>[100,350,900,1800,3500,6500].forEach(t=>setTimeout(cleanAll,t)));
setInterval(cleanAll,650);cleanAll();
})();