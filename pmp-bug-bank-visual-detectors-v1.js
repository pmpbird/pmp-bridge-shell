/* PMP Bug Authority Phase 2A final rebuild from exact main 7f31bf5a5a20f2f779f25b7ddc24dc2e6bf313ed. */
(()=>{'use strict';const V='2.1.0-phase2a-final-visual-observer',OFFSETS=[0,300,900,1800,3500,7000];let scans=0,timers=[];function T(){try{return top||window}catch(_){return window}}function docs(r,a,n){a=a||[];n=n||0;if(!r||n>10)return a;try{a.push(r);r.querySelectorAll('iframe,frame').forEach(f=>{try{let d=f.contentDocument||(f.contentWindow&&f.contentWindow.document);if(d)docs(d,a,n+1)}catch(_){}})}catch(_){}return a}function clean(s){return String(s||'').replace(/\s+/g,' ').trim()}function observe(row){const a=T().PMPBugAuthorityV2||window.PMPBugAuthorityV2;return a&&a.observe?a.observe('bug_bank_visual_detectors',row):Promise.reject(new Error('BUG_AUTHORITY_UNAVAILABLE'))}function detect(d){const out=[],b=d.getElementById('bank');if(!b)return out;const title=clean((b.querySelector('[data-bank-detail-title]')||{}).textContent||'');if(b.querySelector('[data-bug-bank-owner-v1]')&&title!=='Bug Bank')out.push({type:'Cross-Bank Bug Bank Surface Leak',where:'Bank / '+(title||'unknown'),symptom:'Bug Bank UI rendered while a different bank was selected.',severity:'high'});return out}async function scan(){if(scans>=6)throw new Error('BUG_AUTHORITY_SCAN_LIMIT_REACHED');scans++;const rows=[];docs(T().document).forEach(d=>rows.push(...detect(d)));for(const row of rows)await observe(row);return{scan_number:scans,observations:rows.length}}function stop(){timers.forEach(clearTimeout);timers=[];return{stopped:true,scans_executed:scans}}window.PMPBugBankVisualDetectorsV1=Object.freeze({version:V,scan,stop,report:()=>({version:V,scans_executed:scans,bounded_scan_count:6,direct_active_write:false,automatic_repair:false})});timers=OFFSETS.map(ms=>setTimeout(()=>scan().catch(()=>{}),ms));})();
