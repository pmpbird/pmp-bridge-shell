(()=>{
'use strict';
const VERSION='1.0.4-pass5-frozen-path-root-aligned-zip-output';
const REPORT_KEY='pmp_active_path_discovery_report_v1';
const RECEIPT_KEY='pmp_active_path_discovery_receipt_v1';
const ZIP_RECEIPT_KEY='pmp_active_path_discovery_zip_receipt_v1';
function list(s){return Array.from(new Set(String(s||'').trim().split(/\s+/).filter(Boolean)))}
const CURRENT_BOOT_ROOT=list(`
pmp-app-current.html
pmp-current-map-v11.json
pmp-current-map-v10.json
pmp-route-guardian-current-loader-v19.html
pmp-current-reload-owner-v28.html
pmp-current-inner-cleanbug-rgcontrols-v29.html
pmp-current-inner-cleanbug-rgcontrols-v23.html
pmp-current-inner-cleanbug-rgcontrols-v4.html
pmp-current-inner-cleanbug-rgcontrols-v3.html
pmp-home-single-v6.html
`);
const HISTORIC_CURRENT_REFERENCES=list(`
pmp-current-map-v9.json
pmp-current-map.json
pmp-route-guardian-current-loader-v15.html
pmp-route-guardian-current-loader-v17.html
pmp-route-guardian-current-loader-v18.html
pmp-current-reload-owner-v27.html
pmp-current-inner-cleanbug-rgcontrols-v24.html
pmp-current-inner-cleanbug-rgcontrols-v26.html
`);
const ROOT=CURRENT_BOOT_ROOT;
const MAX=220;
let LAST=null;
function topWin(){try{return top||window}catch(e){return window}}
function now(){return new Date().toISOString()}
function cleanPath(x){x=String(x||'').replace(/&amp;/g,'&').trim();x=x.split('#')[0].split('?')[0];x=x.replace(/^\.\//,'').replace(/^\//,'');if(/^https?:/i.test(x)||x.includes('://')||x.includes('..'))return'';let m=x.match(/^[a-zA-Z0-9._\/-]+\.(?:html|js|json)$/i);return m?x:''}
function uniq(a){return Array.from(new Set((a||[]).filter(Boolean))).sort()}
function eachDoc(fn){let seen=[];function walk(w,n){if(!w||n>12||seen.indexOf(w)>-1)return;seen.push(w);try{if(w.document)fn(w.document,w);Array.from(w.document.querySelectorAll('iframe,frame')).forEach(f=>{try{walk(f.contentWindow,n+1)}catch(e){}})}catch(e){}}walk(window,0);try{walk(topWin(),0)}catch(e){}}
function liveFiles(){let out=[];eachDoc((d,w)=>{try{out.push(cleanPath(String(w.location&&w.location.pathname||'').split('/').pop()));Array.from(d.querySelectorAll('script[src],iframe[src],frame[src],a[href]')).forEach(el=>out.push(cleanPath(el.getAttribute('src')||el.getAttribute('href')||'')))}catch(e){}});return uniq(out)}
function atlasPaths(){let r=null;try{if(window.PMPMountRegistryV1&&typeof window.PMPMountRegistryV1.registry==='function')r=window.PMPMountRegistryV1.registry()}catch(e){}if(!r){try{r=JSON.parse(localStorage.getItem('pmp_mount_registry_v1')||'null')}catch(e){}}
let files=[];if(r&&Array.isArray(r.files))files=r.files.map(f=>f&&f.path).filter(Boolean);if(r&&r.repo_file_classification){Object.keys(r.repo_file_classification).forEach(k=>{let a=r.repo_file_classification[k];if(Array.isArray(a))files=files.concat(a)})}return uniq(files.map(cleanPath))}
function extract(text){let out=[];String(text||'').replace(/['"`(=\s]([a-zA-Z0-9._\/-]+\.(?:html|js|json))(?=$|[?#'"`\s)>,;])/gi,(m,p)=>{out.push(cleanPath(p));return m});String(text||'').replace(/(?:src|href)\s*=\s*['"]([^'"]+)['"]/gi,(m,p)=>{out.push(cleanPath(p));return m});return uniq(out)}
function lane(path,from){let p=String(path||'').toLowerCase(),f=String(from||'').toLowerCase();if(/test|candidate|repo-index|route-health|path-certifier|simulation|diagnostic|archive|history|old-panel|last-known|legacy/.test(p))return'legacy_or_test_candidate';if(/last-good|recovery|restore|safe-writer|code-safety|safety|resident|clean-v21|clean-v22/.test(p)||/last-good|recovery/.test(f))return'fallback_or_recovery_candidate';if(CURRENT_BOOT_ROOT.indexOf(path)>-1)return'current_boot_root';return'direct_current_candidate'}
function canFollow(path,depth,sourceLane){if(depth>2)return false;if(/legacy_or_test/.test(sourceLane))return false;if(/fallback_or_recovery/.test(sourceLane)&&depth>1)return false;if(/current-inner\.html|home-single-v1[024]|route-guardian-v1|current-inner-cleanbug-rgcontrols-v(2|9|13|16)\.html|index\.html|bm\.html|private-bug|resident-notes|zip-xray/.test(path))return false;return true}
async function fetchText(path){let r=await fetch(path+'?active_path_discovery='+Date.now(),{cache:'no-store'});let text=await r.text().catch(()=>'');return{ok:r.ok,status:r.status,text}}
async function run(){let started=now(),atlas=atlasPaths(),live=liveFiles(),q=[],seen={},edges=[],rows=[],dead=[];function push(path,from,depth){path=cleanPath(path);if(!path||seen[path]||q.some(x=>x.path===path))return;let ln=from?'':(CURRENT_BOOT_ROOT.indexOf(path)>-1?'current_boot_root':'direct_current_candidate');if(from)ln=lane(path,from);q.push({path,from:from||'',depth:depth||0,lane:ln})}
ROOT.concat(live).forEach(p=>push(p,'',0));while(q.length&&rows.length<MAX){let item=q.shift(),path=item.path;if(!path||seen[path])continue;seen[path]=true;let rec={path,from:item.from,depth:item.depth,lane:item.lane,ok:false,status:0,found:[],in_atlas:atlas.indexOf(path)>-1,live_now:live.indexOf(path)>-1};try{let got=await fetchText(path);rec.ok=got.ok;rec.status=got.status;if(got.ok){rec.found=extract(got.text);rec.found.forEach(p=>{let ln=lane(p,path);edges.push({from:path,to:p,lane:ln});if(canFollow(p,item.depth+1,ln))push(p,path,item.depth+1)})}else dead.push({path,status:got.status,lane:item.lane,from:item.from})}catch(e){rec.error=String(e&&e.message||e);dead.push({path,error:rec.error,lane:item.lane,from:item.from})}rows.push(rec)}let reachable=uniq(rows.filter(r=>r.ok).map(r=>r.path));let liveMissing=live.filter(p=>atlas.indexOf(p)<0);let directMissing=rows.filter(r=>r.ok&&r.lane==='direct_current_candidate'&&atlas.indexOf(r.path)<0).map(r=>r.path);let fallbackMissing=rows.filter(r=>r.ok&&r.lane==='fallback_or_recovery_candidate'&&atlas.indexOf(r.path)<0).map(r=>r.path);let legacyMissing=rows.filter(r=>r.ok&&r.lane==='legacy_or_test_candidate'&&atlas.indexOf(r.path)<0).map(r=>r.path);let hard=uniq(liveMissing.concat(directMissing));let oldAsRoot=uniq(rows.filter(r=>HISTORIC_CURRENT_REFERENCES.indexOf(r.path)>-1&&r.lane==='current_boot_root').map(r=>r.path));let report={type:'PMP_ACTIVE_PATH_DISCOVERY_REPORT_V1',version:VERSION,owner:'pmp-active-path-discovery-machine-v1',started_at:started,finished_at:now(),mode:'passive_discovery_only',rule:'Reads active app files and records a report only. No fixing, moving, deleting, Bank rebuild, route change, or app data migration.',pass5_alignment:{goal:'Frozen path truth alignment only. Old v9/v15/v27/v24/v26 route references may remain reachable but must not be current_boot_root authority.',current_boot_root:CURRENT_BOOT_ROOT,historic_current_references_not_boot_root:HISTORIC_CURRENT_REFERENCES,historic_reference_current_boot_root_count:oldAsRoot.length,historic_reference_current_boot_root:oldAsRoot},scanned_count:rows.length,reachable_count:reachable.length,atlas_count:atlas.length,hard_missing_count:hard.length,live_runtime_missing_count:liveMissing.length,direct_current_missing_count:uniq(directMissing).length,fallback_or_recovery_missing_count:uniq(fallbackMissing).length,legacy_or_test_missing_count:uniq(legacyMissing).length,dead_reference_count:dead.length,hard_missing:hard,live_runtime_missing:liveMissing,direct_current_missing:uniq(directMissing),fallback_or_recovery_missing:uniq(fallbackMissing),legacy_or_test_missing:uniq(legacyMissing),dead_references:dead,reachable_files:reachable,live_files:live,edges,scanned:rows,freeze_gate:{pass:hard.length===0&&oldAsRoot.length===0,rule:'Freeze only if hard_missing_count is zero and historic current references are not classified as current_boot_root. Fallback/recovery and legacy/test lanes require manual classification, not automatic Atlas authority.'}};LAST=report;try{localStorage.setItem(REPORT_KEY,JSON.stringify(report,null,2));localStorage.setItem(RECEIPT_KEY,JSON.stringify({type:'PMP_ACTIVE_PATH_DISCOVERY_RECEIPT_V1',version:VERSION,at:now(),scanned_count:rows.length,hard_missing_count:hard.length,live_runtime_missing_count:liveMissing.length,direct_current_missing_count:uniq(directMissing).length,fallback_or_recovery_missing_count:uniq(fallbackMissing).length,legacy_or_test_missing_count:uniq(legacyMissing).length,dead_reference_count:dead.length,historic_reference_current_boot_root_count:oldAsRoot.length,freeze_gate_pass:report.freeze_gate.pass,zip_export_available:true},null,2))}catch(e){}window.PMPActivePathDiscoveryReportV1=report;mount();return report}
function copyText(s){return navigator.clipboard&&navigator.clipboard.writeText?navigator.clipboard.writeText(s):Promise.reject(Error('clipboard unavailable'))}
function button(d,text,fn){let b=d.createElement('button');b.type='button';b.textContent=text;b.style.cssText='width:100%;border:2px solid var(--line,#07101c);border-radius:16px;padding:12px;margin:8px 0;background:var(--a,#acd1fb);color:var(--buttonText,#101827);font:inherit;font-weight:950;text-align:left';b.onclick=fn;return b}
function summary(r){if(!r)return'Waiting for discovery scan...';let old=(r.pass5_alignment&&typeof r.pass5_alignment.historic_reference_current_boot_root_count==='number')?' · old-as-root: '+r.pass5_alignment.historic_reference_current_boot_root_count:'';return 'hard missing: '+r.hard_missing_count+' · live: '+r.live_runtime_missing_count+' · direct: '+r.direct_current_missing_count+' · fallback: '+r.fallback_or_recovery_missing_count+' · legacy/test: '+r.legacy_or_test_missing_count+' · dead: '+r.dead_reference_count+old}
function zipSummary(r){return [
'PMP Active Path Discovery ZIP',
'created_at: '+now(),
'version: '+VERSION,
'summary: '+summary(r),
'freeze_gate_pass: '+(r&&r.freeze_gate?r.freeze_gate.pass:false),
'mode: passive discovery only',
'rule: upload this ZIP to ChatGPT instead of pasting the long report'
].join('\n')+'\n'}
function enc(s){return new TextEncoder().encode(String(s||''))}
const CRC_TABLE=(()=>{let t=[];for(let n=0;n<256;n++){let c=n;for(let k=0;k<8;k++)c=(c&1)?(0xedb88320^(c>>>1)):(c>>>1);t[n]=c>>>0}return t})();
function crc32(bytes){let c=0xffffffff;for(let i=0;i<bytes.length;i++)c=CRC_TABLE[(c^bytes[i])&255]^(c>>>8);return(c^0xffffffff)>>>0}
function dosDateTime(){let d=new Date(),yr=Math.max(1980,d.getFullYear());return{time:((d.getHours()&31)<<11)|((d.getMinutes()&63)<<5)|((Math.floor(d.getSeconds()/2))&31),date:(((yr-1980)&127)<<9)|(((d.getMonth()+1)&15)<<5)|(d.getDate()&31)}}
function p16(a,v){a.push(v&255,(v>>>8)&255)}
function p32(a,v){a.push(v&255,(v>>>8)&255,(v>>>16)&255,(v>>>24)&255)}
function zipBlob(files){let parts=[],central=[],offset=0,dt=dosDateTime();files.forEach(f=>{let name=enc(f.name),data=enc(f.data),crc=crc32(data),lh=[];p32(lh,0x04034b50);p16(lh,20);p16(lh,0);p16(lh,0);p16(lh,dt.time);p16(lh,dt.date);p32(lh,crc);p32(lh,data.length);p32(lh,data.length);p16(lh,name.length);p16(lh,0);parts.push(new Uint8Array(lh),name,data);let ch=[];p32(ch,0x02014b50);p16(ch,20);p16(ch,20);p16(ch,0);p16(ch,0);p16(ch,dt.time);p16(ch,dt.date);p32(ch,crc);p32(ch,data.length);p32(ch,data.length);p16(ch,name.length);p16(ch,0);p16(ch,0);p16(ch,0);p16(ch,0);p32(ch,0);p32(ch,offset);central.push(new Uint8Array(ch),name);offset+=lh.length+name.length+data.length});let cdSize=central.reduce((n,p)=>n+p.length,0),cdOffset=offset,end=[];p32(end,0x06054b50);p16(end,0);p16(end,0);p16(end,files.length);p16(end,files.length);p32(end,cdSize);p32(end,cdOffset);p16(end,0);return new Blob(parts.concat(central,[new Uint8Array(end)]),{type:'application/zip'})}
function buildZip(r){let receipt={type:'PMP_ACTIVE_PATH_DISCOVERY_ZIP_RECEIPT_V1',version:VERSION,at:now(),file_name:'pmp-active-path-discovery-report.zip',summary:summary(r),freeze_gate_pass:!!(r&&r.freeze_gate&&r.freeze_gate.pass),hard_missing_count:r?r.hard_missing_count:null,dead_reference_count:r?r.dead_reference_count:null};let files=[{name:'README.txt',data:'This ZIP replaces the oversized pasted discovery report. Upload this ZIP to ChatGPT.\n\n'+zipSummary(r)},{name:'discovery-summary.txt',data:zipSummary(r)},{name:'discovery-report.json',data:JSON.stringify(r,null,2)},{name:'discovery-zip-receipt.json',data:JSON.stringify(receipt,null,2)},{name:'reachable-files.txt',data:(r&&r.reachable_files?r.reachable_files:[]).join('\n')+'\n'},{name:'dead-references.json',data:JSON.stringify(r&&r.dead_references?r.dead_references:[],null,2)}];return{blob:zipBlob(files),receipt}}
function downloadZip(d,r,out){let made=buildZip(r),url=URL.createObjectURL(made.blob),a=d.createElement('a');a.href=url;a.download='pmp-active-path-discovery-report.zip';a.rel='noopener';a.style.display='none';d.body.appendChild(a);a.click();setTimeout(()=>{try{URL.revokeObjectURL(url);a.remove()}catch(e){}},1500);try{localStorage.setItem(ZIP_RECEIPT_KEY,JSON.stringify(made.receipt,null,2));window.PMPActivePathDiscoveryZipReceiptV1=made.receipt}catch(e){}if(out)out.textContent='Discovery report ZIP created. Upload the ZIP to ChatGPT instead of pasting the long report.'}
function mount(){let report=LAST;eachDoc(d=>{try{let old=d.getElementById('pmpPass1YDiscoveryCardV1');if(old)old.remove();let existing=d.getElementById('pmpActivePathDiscoveryCardV1');let target=d.querySelector('#control .card')||d.querySelector('[data-screen="control"] .card')||d.querySelector('#control')||null;if(!target)return;if(existing){let out=existing.querySelector('#pmpActivePathDiscoveryOutV1');if(out)out.textContent=summary(report);let btn=existing.querySelector('[data-pmp-discovery-zip-button="1"]');if(btn)btn.textContent='Copy Discovery Report ZIP';return}let card=d.createElement('div');card.id='pmpActivePathDiscoveryCardV1';card.style.cssText='background:var(--card,#fff);color:var(--text,#101827);border:2px solid var(--line,#07101c);border-radius:20px;padding:12px;margin:10px 0;white-space:pre-wrap';let title=d.createElement('b');title.textContent='Active Path Discovery';let out=d.createElement('div');out.id='pmpActivePathDiscoveryOutV1';out.textContent=summary(report);card.appendChild(title);card.appendChild(out);let zipButton=button(d,'Copy Discovery Report ZIP',async()=>{let r=window.PMPActivePathDiscoveryReportV1||LAST;if(!r)r=await run();try{downloadZip(d,r,out)}catch(e){let s=JSON.stringify(r,null,2);try{await copyText(s);out.textContent='ZIP export failed; JSON report copied as fallback.'}catch(e2){out.textContent=s}}});zipButton.setAttribute('data-pmp-discovery-zip-button','1');card.appendChild(zipButton);card.appendChild(button(d,'Run Discovery Again',async()=>{out.textContent='Running discovery...';let r=await run();out.textContent=summary(r)}));target.appendChild(card)}catch(e){}})}
window.PMPActivePathDiscoveryMachineV1={version:VERSION,run,mount,reportKey:REPORT_KEY,receiptKey:RECEIPT_KEY,zipReceiptKey:ZIP_RECEIPT_KEY,currentBootRoot:CURRENT_BOOT_ROOT,historicCurrentReferences:HISTORIC_CURRENT_REFERENCES};
setTimeout(run,3500);
[1000,2500,4500,7000,10000,15000,22000,30000,45000,60000].forEach(t=>setTimeout(mount,t));
setInterval(mount,5000);
})();
