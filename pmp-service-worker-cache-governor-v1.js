'use strict';
const PMP_SW_VERSION='1.0.0-pass75-service-worker-cache-governor-20260708A';
const PMP_CACHE_PREFIXES=['pmp-','p03-'];
const PMP_SAFE_CACHE='pmp-runtime-cache-governor-v1';
function receipt(reason,extra){return Object.assign({type:'PMP_SERVICE_WORKER_CACHE_GOVERNOR_RECEIPT_V1',version:PMP_SW_VERSION,reason,at:new Date().toISOString(),cache_governor:true,network_first_html:true,safe_cache_prefixes:PMP_CACHE_PREFIXES,does_not_touch:['IndexedDB','localStorage','Bank data','user content']},extra||{})}
async function postAll(msg){const cs=await self.clients.matchAll({includeUncontrolled:true,type:'window'});cs.forEach(c=>{try{c.postMessage(msg)}catch(e){}})}
self.addEventListener('install',event=>{event.waitUntil((async()=>{self.skipWaiting();await postAll({type:'PMP_SW_INSTALLED',receipt:receipt('install')})})())});
self.addEventListener('activate',event=>{event.waitUntil((async()=>{await self.clients.claim();await postAll({type:'PMP_SW_ACTIVATED',receipt:receipt('activate')})})())});
self.addEventListener('message',event=>{const d=event.data||{};if(d.type==='PMP_SW_STATUS_REQUEST'){event.source&&event.source.postMessage({type:'PMP_SW_STATUS_RESPONSE',receipt:receipt('status')});return}if(d.type==='PMP_SW_RUNTIME_CACHE_RESET'){event.waitUntil((async()=>{const keys=await caches.keys();const deleted=[];for(const k of keys){if(PMP_CACHE_PREFIXES.some(p=>String(k).startsWith(p))){await caches.delete(k);deleted.push(k)}}event.source&&event.source.postMessage({type:'PMP_SW_RUNTIME_CACHE_RESET_DONE',receipt:receipt('runtime_cache_reset',{deleted_caches:deleted})})})())}});
self.addEventListener('fetch',event=>{const req=event.request;if(req.method!=='GET')return;const url=new URL(req.url);if(url.origin!==location.origin)return;const isHtml=req.mode==='navigate'||url.pathname.endsWith('.html');if(isHtml){event.respondWith((async()=>{try{const fresh=await fetch(req,{cache:'no-store'});const c=await caches.open(PMP_SAFE_CACHE);c.put(req,fresh.clone()).catch(()=>{});return fresh}catch(e){const cached=await caches.match(req);if(cached)return cached;throw e}})());return}event.respondWith((async()=>{const cached=await caches.match(req);if(cached)return cached;const fresh=await fetch(req);try{const c=await caches.open(PMP_SAFE_CACHE);c.put(req,fresh.clone())}catch(e){}return fresh})())});