# Pass 2 P2-B draft publication scope

This branch is temporarily carrying the self-removing exact patch publisher. The publisher must replace all staging files with the intended P2-B enforcement patch before certification. This scope marker is intentionally outside the publisher path trigger and will be deleted before the final P2-B certification run.
