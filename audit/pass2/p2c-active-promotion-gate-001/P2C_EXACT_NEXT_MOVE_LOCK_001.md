# P2-C Exact Next Move Lock 001

After this gate is green and merged, the exact next move is one fresh formal-proof pull request whose final and only head commit change is:

`audit/pass2/p2c-isolated-proof-rerun-008/P2C_RECEIPT082_FORMAL_PROOF_PR_HEAD_SEAL_084.json`

The seal must bind its exact parent commit and the current Directive 083 and Receipt 082 bytes. The Receipt 038 static preflight and every Receipt 082/Directive 083 workflow, wrapper, controller, patcher, finalizer, and base-tree binding must pass before the controller begins.

Exactly one formal proof may execute. Do not dispatch or rerun it manually. If it consumes the authorization and fails, preserve the complete evidence and stop at that gate.

If it passes, preserve its evidence and proceed to a separate rollback-ready production promotion branch for enforced Authority Rules and bounded passive Bug Watch. Production promotion must preserve Current Map destinations, Safe Writer current return, persisted user data, and the no-automatic-repair rule.

This lock does not itself authorize production application, production activation, a second proof run, persisted-data changes, or Pass 3.
