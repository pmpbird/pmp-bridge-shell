# Automated Plan Foundation UI Retirement v1

Status: **FORMALLY RETIRED AS CURRENT-APP UI ASSERTION — NON-EXECUTABLE FOUNDATION CONTRACT PRESERVED**

The original Automated Plan foundation remains preserved as a historical, non-executable, zero-additional-cost contract:

- plan identity: `packet_01_5`
- last completed boundary: `pass_002`
- next declared boundary: `pass_003`
- execution enabled: no
- autonomous trigger enabled: no
- write authority: none
- merge authority: none
- spending ceiling: `$0`
- paid API and paid fallback: forbidden
- backends: Hosted Free and Laptop under the same checkpoint and result contracts

The old live UI assertions are retired because they no longer describe the current application architecture:

- `pmp-current-inner-cleanbug-rgcontrols-v6.html` is retained as a historical wrapper, not Current Map authority.
- Current application authority is `pmp-current-reload-owner-v30-direct-boot-surface-20260708A.html` through `pmp-current-map-v12.json`.
- Current runtime requires A-003 SHA-256 source-byte enforcement.
- The old `Automated Plan` visual owner was superseded by the `Continuous Run Dashboard` owner while retaining the historical DOM anchor `pmpAutomatedPlanEntryV1`.
- The current Home path is an A-003 verified historical-payload restore shim, so static assertions that the shim itself contains Control Room markup are invalid.

The replacement merge check must verify:

1. the historical zero-cost and authority contracts remain unchanged and disabled;
2. the superseding Continuous Run Dashboard owner and native-theme matcher remain structurally present;
3. the legacy v6 wrapper is not current route authority;
4. the current A-003 app boots successfully and exposes its Control Room through the certified route chain;
5. no claim is made that the retired v6 UI remains the current app.

This retirement does not start Pass 003, enable automation, grant write or merge authority, or alter any runtime source byte.
