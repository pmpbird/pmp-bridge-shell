# Whole App Health event-window repair

This repair restores the single white scrolling Whole App Health report window without restoring the late recurring layout writer. The declared Diagnostics presentation helper observes owner renders and performs one idempotent wrap before paint. It never writes Diagnostics screen padding, changes diagnostic content, changes routes or authority, migrates storage, or writes persisted user data.
