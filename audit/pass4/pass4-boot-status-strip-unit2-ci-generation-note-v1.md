# Pass 4 Unit 2 CI generation note

The dedicated Unit 2 workflow deterministically applies the bounded passive strip integration and commits only the four required generated runtime/integrity paths before exact-scope verification.
