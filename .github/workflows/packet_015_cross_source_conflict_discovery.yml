name: Packet 015 cross source conflict discovery

on:
  pull_request:
    paths:
      - "tools/packet_01_5_cross_source_targeted_metadata.py"
      - ".github/workflows/packet_015_cross_source_conflict_discovery.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  discover:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python3 tools/packet_01_5_cross_source_targeted_metadata.py
      - uses: actions/upload-artifact@v4
        with:
          name: packet-015-cross-source-conflict-targeted-metadata
          path: audit/Packet_01.5_Cross_Source_Conflict_Targeted_Metadata_v1.json
          if-no-files-found: error
