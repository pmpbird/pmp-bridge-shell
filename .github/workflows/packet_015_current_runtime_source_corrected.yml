name: Packet 015 corrected current runtime source

on: [push]

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: python3 tools/verify_packet_01_5_runtime_source_corrected_v1.py
